name: NekoJS CI Build

on:
  push:
    branches:
      - main
      - master
  pull_request:
    branches:
      - main
      - master
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  analyze:
    name: Analyze build metadata
    runs-on: ubuntu-latest
    outputs:
      is_release: ${{ steps.meta.outputs.is_release }}
      version: ${{ steps.meta.outputs.version }}
      title: ${{ steps.meta.outputs.title }}
      short_sha: ${{ steps.meta.outputs.short_sha }}
    steps:
      - name: Checkout metadata
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Extract commit title and release version
        id: meta
        shell: bash
        env:
          RAW_COMMIT_MSG: ${{ github.event.head_commit.message || github.event.pull_request.title || github.event_name }}
        run: |
          set -euo pipefail
          shopt -s nocasematch

          TITLE="$(printf '%s' "$RAW_COMMIT_MSG" | sed -n '1p')"
          SHORT_SHA="$(git rev-parse --short HEAD)"
          REGEX='^update[[:space:]]+([0-9A-Za-z][0-9A-Za-z._-]*)$'

          echo "title=$TITLE" >> "$GITHUB_OUTPUT"
          echo "short_sha=$SHORT_SHA" >> "$GITHUB_OUTPUT"

          if [[ "$TITLE" =~ $REGEX ]]; then
            echo "is_release=true" >> "$GITHUB_OUTPUT"
            echo "version=${BASH_REMATCH[1]}" >> "$GITHUB_OUTPUT"
            echo "Release build detected from commit title: $TITLE"
          else
            echo "is_release=false" >> "$GITHUB_OUTPUT"
            echo "version=" >> "$GITHUB_OUTPUT"
            echo "Development build detected from commit title: $TITLE"
          fi

  build:
    name: Build platform modules
    needs: analyze
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up JDK 25
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '25'

      - name: Set up Node.js 22
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: npm

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v4
        with:
          # main/master 写入缓存；PR 分支只读，避免污染缓存
          cache-read-only: ${{ github.ref != 'refs/heads/main' && github.ref != 'refs/heads/master' }}

      - name: Make gradlew executable
        run: chmod +x ./gradlew

      - name: Validate Gradle wrapper
        uses: gradle/actions/wrapper-validation@v4

      - name: Install Probe typecheck dependencies
        run: npm ci

      - name: Run common checks
        # 用 :common:check 而非 :common:test：check 才会挂上 checkCommonIsolation 隔离门禁
        # :common-api-processor:test 显式列出（JavaPlugin 的 check 默认已依赖 test，这里点名
        # 是防止将来 check 挂载被改掉时单测静默丢失）
        run: ./gradlew :common-api:check :common:check :common-api-processor:test --console=plain

      - name: Run platform NBT smoke tests
        # :common:test 已覆盖 common 的 nbt-smoke 标签用例，这里只补平台侧（platforms/* 的测试否则永不运行）
        run: ./gradlew nbtSmokeTest -x :common:nbtSmokeTest --console=plain

      - name: Typecheck Probe declarations
        run: npm run test:probe-types

      - name: Discover platform modules
        id: platforms
        shell: bash
        run: |
          set -euo pipefail
          BUILD_TASKS=""
          # 以 settings.gradle 的 include 列表为唯一事实来源，避免 find 探测与模块声明漂移
          while IFS= read -r name; do
            BUILD_TASKS="$BUILD_TASKS :platforms:$name:build"
          done < <(grep -o "include 'platforms:[^']*'" settings.gradle | sed "s/include 'platforms://;s/'//" | sort -u)

          if [[ -z "$BUILD_TASKS" ]]; then
            echo "No platform modules found in settings.gradle" >&2
            exit 1
          fi

          echo "build_tasks=$BUILD_TASKS" >> "$GITHUB_OUTPUT"
          echo "Discovered build tasks:$BUILD_TASKS"

      - name: Build platform modules
        # 单个 build 已按依赖链覆盖 compileJava → test → check（隔离门禁/verifyRuntimeArtifact/
        # verifyDevModSourceSets），无需分三步重复调用 Gradle
        run: ./gradlew ${{ steps.platforms.outputs.build_tasks }} --console=plain

      - name: Prepare artifacts
        shell: bash
        env:
          IS_RELEASE: ${{ needs.analyze.outputs.is_release }}
          SHORT_SHA: ${{ needs.analyze.outputs.short_sha }}
        run: |
          set -euo pipefail
          mkdir -p artifacts

          while IFS= read -r jar; do
            name="$(basename "$jar")"
            case "$name" in
              *-sources.jar|*-javadoc.jar|*-dev.jar|*-plain.jar) continue ;;
            esac

            if [[ "$IS_RELEASE" == "true" ]]; then
              cp "$jar" "artifacts/$name"
            else
              cp "$jar" "artifacts/${name%.jar}-dev-${SHORT_SHA}.jar"
            fi
          done < <(find platforms -path '*/build/libs/*.jar' -type f | sort)

          if ! compgen -G 'artifacts/*.jar' > /dev/null; then
            echo "No jar artifacts were produced" >&2
            exit 1
          fi

          ls -lh artifacts

      - name: Upload build artifacts
        uses: actions/upload-artifact@v4
        with:
          name: ${{ needs.analyze.outputs.is_release == 'true' && 'NekoJS-Release-Jars' || 'NekoJS-Dev-Jars' }}
          path: artifacts/*.jar
          if-no-files-found: error
          retention-days: ${{ needs.analyze.outputs.is_release == 'true' && 90 || 14 }}

      - name: Upload test reports on failure
        # 放在 job 末尾 + if: ${{ !cancelled() && failure() }}：任何一步失败（common 检查 / 平台 NBT 冒烟 /
        # 平台测试 / 打包）都能带上报表；!cancelled() 保证并发取消后仍尽力补传已有报表
        if: ${{ !cancelled() && failure() }}
        uses: actions/upload-artifact@v4
        with:
          name: test-reports
          path: |
            common/build/reports/tests/test
            common/build/test-results/test
            platforms/*/build/reports/tests/test
            platforms/*/build/test-results
          if-no-files-found: ignore

  common-jdk21:
    name: Common checks (JDK 21)
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '21'

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v4
        with:
          # 独立小作业只读缓存，避免与主构建写入的缓存条目互相挤占
          cache-read-only: true

      - name: Make gradlew executable
        run: chmod +x ./gradlew

      - name: Run common checks
        # 主 job 的 Gradle 守护进程跑在 JDK 25 上；这里用 JDK 21 跑同一组检查（工具链同为 21），
        # 捕捉对守护进程 JVM 版本的隐性依赖（toolchain 漂移）；任务列表与主 job 保持一致
        run: ./gradlew :common-api:check :common:check :common-api-processor:test --console=plain
  release:
    name: Create GitHub release
    needs:
      - analyze
      - build
    if: github.event_name == 'push' && needs.analyze.outputs.is_release == 'true'
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Download release jars
        uses: actions/download-artifact@v4
        with:
          name: NekoJS-Release-Jars
          path: artifacts

      - name: Generate changelog
        id: changelog
        shell: bash
        env:
          COMMIT_TITLE: ${{ needs.analyze.outputs.title }}
        run: |
          set -euo pipefail
          PREVIOUS_TAG="$(git describe --tags --abbrev=0 2>/dev/null || true)"

          if [[ -z "$PREVIOUS_TAG" ]]; then
            RANGE="HEAD"
          else
            RANGE="${PREVIOUS_TAG}..HEAD"
          fi

          NOTES="$(git log "$RANGE" --pretty=format:'* %s (%h)' | sed '/^$/d')"
          if [[ -z "$NOTES" ]]; then
            NOTES="* ${COMMIT_TITLE}"
          fi

          EOF_MARKER="CHANGELOG_$(date +%s)_$RANDOM"
          echo "notes<<$EOF_MARKER" >> "$GITHUB_OUTPUT"
          echo "$NOTES" >> "$GITHUB_OUTPUT"
          echo "$EOF_MARKER" >> "$GITHUB_OUTPUT"

      - name: Create GitHub release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: v${{ needs.analyze.outputs.version }}
          name: NekoJS v${{ needs.analyze.outputs.version }}
          body: |
            ## Summary
            ${{ needs.analyze.outputs.title }}

            ## Changelog
            ${{ steps.changelog.outputs.notes }}
          files: artifacts/*.jar
          fail_on_unmatched_files: true
